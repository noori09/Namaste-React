Q1. What is JSX?
Ans- JSX is javascript XML. It allows us to write HTMl like syntax in React.

Q2. What are the super powers of React?
Ans- It helps us in keeping our code simpler and elegant when writing large piece of code.
     JSX allows react to show more useful errors and warning messages.

Q3. Role of type attribute in script tag? What options can be used?
Ans- The type attribute specifies the type of script. It has a default value - text/javascript
